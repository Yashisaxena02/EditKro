// Editkaro.in portfolio is a pure HTML/CSS/JS site served from /public.
// This React entry is intentionally a no-op so it never interferes with the
// static site (index.html -> style.css + script.js).
const mount = document.getElementById("react-root");
if (mount) {
  // reserved: no React app is mounted for this static portfolio project.
}
export {};
